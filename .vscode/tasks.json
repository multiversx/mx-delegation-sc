{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "auction-mock-debug-build",
            "command": "cargo",
            "args": [
                "build"
            ],
            "options": {
                "cwd": "${workspaceFolder}//auction-mock/debug"
            },
            "type": "shell"
        },
        {
            "label": "latest_full-debug-build",
            "command": "cargo",
            "args": [
                "build"
            ],
            "options": {
                "cwd": "${workspaceFolder}//latest_full/debug"
            },
            "type": "shell"
        },
        {
            "label": "latest_update-debug-build",
            "command": "cargo",
            "args": [
                "build"
            ],
            "options": {
                "cwd": "${workspaceFolder}//latest_update/debug"
            },
            "type": "shell"
        }
    ]
}