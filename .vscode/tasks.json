{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "auction-mock-debug-build",
            "command": "cargo",
            "args": [
                "build"
            ],
            "options": {
                "cwd": "${workspaceFolder}//auction-mock/debug"
            },
            "type": "shell"
        },
        {
            "label": "sc-delegation-rs-debug-build",
            "command": "cargo",
            "args": [
                "build"
            ],
            "options": {
                "cwd": "${workspaceFolder}//debug"
            },
            "type": "shell"
        }
    ]
}