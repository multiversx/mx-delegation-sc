{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "user-fund-storage-debug-build",
            "command": "cargo",
            "args": [
                "build"
            ],
            "options": {
                "cwd": "${workspaceFolder}//user-fund-storage/debug"
            },
            "type": "shell"
        }
    ]
}