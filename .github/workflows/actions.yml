name: CI

on:
  push:
    branches:
      - master
  pull_request:

permissions:
  checks: write
  pull-requests: write

jobs:
  contracts:
    name: Contracts
    uses: multiversx/mx-sc-actions/.github/workflows/contracts.yml@0da5080fcdd73474bf1fabbacad8c79e8aba74b6
    with:
      rust-toolchain: stable
      mx-scenario-go-version: v5.1.0
    secrets:
      token: ${{ secrets.GITHUB_TOKEN }}
